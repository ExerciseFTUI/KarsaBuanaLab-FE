import React, { FC } from "react";
import VerticalCheckbox from "../ui/VerticalCheckbox";

interface SampleProps {

}

const Sample: FC<SampleProps> = () => {
    return (
        <VerticalCheckbox/>
    );
};

export default Sample;
